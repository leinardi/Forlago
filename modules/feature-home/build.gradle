apply from: rootProject.file('gradle/base-feature.gradle')

android {
    resourcePrefix 'home_'
    defaultConfig {
        consumerProguardFiles "$projectDir/proguard-home-consumer-rules.pro"
    }
}
